.next
node_modules
data
reports
out
package-lock.json
*.tsbuildinfo
# Next.js rewrites tsconfig.json on every build, so it is not Prettier-enforced.
tsconfig.json

# Hand-authored docs and fixtures are maintained by hand, not reflowed.
*.md
seed/**
